import React, { FC } from 'react';
import type { IconProps, Props, PropTypes } from "../typescript";



const HomeBackground: FC<PropTypes<Props, IconProps>> = (props) => {
    return (
        <>
            HomeBackground
        </>
    );
}
export default HomeBackground;